-- Smart Vending Pharmacy (Kenya) - Database Creation Script
-- Run this file against your PostgreSQL server as a user with permission to CREATE DATABASE.
--
-- Usage example (psql):
--   psql -U postgres -f db/postgres/create_database.sql
--
-- If the database already exists, you can drop it manually (do not do this in production).

CREATE DATABASE smart_vending_pharmacy;

